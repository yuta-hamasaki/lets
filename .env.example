# =============================================
# StartPack 環境変数テンプレート (.env.example)
# =============================================
# このファイルをプロジェクト直下に `.env` としてコピーし、値を設定してください。
# 例: cp .env.example .env
#
# 注意:
# - 値はすべてサンプルです。実際の値に置き換えてください。
# - 開発環境と本番環境で値が異なります（特に Stripe / Resend）。
# - Next.js / Prisma は .env を自動読み込みします。

# =====================
# アプリ設定
# =====================
# アプリのベースURL（ローカル開発）
NEXT_PUBLIC_APP_URL=http://localhost:3002

# =====================
# データベース（Neon 推奨）
# =====================
# Neon のダッシュボードから "Connection string" をコピーして設定
# ※ ?sslmode=require を必ず付与
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# =====================
# 認証（Neon Auth / Stack Auth）
# =====================
# Neon Auth の Configuration からコピーして設定
NEXT_PUBLIC_STACK_PROJECT_ID=your-project-id
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=your-publishable-client-key
STACK_SECRET_SERVER_KEY=your-secret-server-key

# =====================
# 決済（Stripe）
# =====================
# 開発では Stripe のテストモード（サンドボックス）を使用してください。
# キーの接頭辞: sk_test_ / pk_test_
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# サブスクリプションの Price ID（継続・毎月）。テストモード側で作成してコピー
STRIPE_PRICE_ID=price_...

# Webhook シークレット。
# 開発では Stripe CLI で `stripe listen --forward-to localhost:3002/api/stripe/webhook` を実行し、
# 表示されたシークレットを設定（whsec_...）
STRIPE_WEBHOOK_SECRET=whsec_...

# =====================
# メール（Resend）
# =====================
# Resend の API キー
RESEND_API_KEY=re_...

# 受信先メール（CONTACT_EMAIL）
# 開発（ドメイン未検証）: Resend アカウント所有者のメールアドレスのみ送信可能です。
# ここにはあなたの Resend アカウントのメールを設定してください（例: you@example.com）。
CONTACT_EMAIL=you@example.com

# 送信元（本番/検証済みドメインのみ）
# 本番でドメイン検証（SPF/DKIM）を完了後、独自ドメインの送信元を使用します。
# 開発では設定しないでください（onboarding@resend.dev が使用されます）。
# RESEND_FROM=support@yourdomain.com

# Resend の送信ドメイン（任意。本番で独自ドメイン送信を行う場合）
# RESEND_DOMAIN=yourdomain.com

# =====================
# お問い合わせAPI向け（任意）
# =====================
# レート制限（固定ウィンドウ）
CONTACT_RATE_LIMIT_WINDOW_MS=60000
CONTACT_RATE_LIMIT_MAX=5

# 開発中はメール送信をスキップしたい場合（true にすると実送信しません）
# SKIP_EMAIL_SEND=true
